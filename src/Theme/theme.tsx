import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6200ea", // Color principal
    },
    secondary: {
      main: "#03dac6", // Color secundario
    },
  },
});

export default theme;